Replace these sample SVG files with the real QR images you receive.

Keep the filenames aligned with `data/db.json`:

- `full-stack-web.svg`
- `data-analytics.svg`
- `ai-productivity.svg`

If your real images are PNG or JPG, update the matching course `qrImage` value in `data/db.json`.
